# S3b behavior parity table — source `py-projects` vs port `ts-projects`

> **Temporary location**: this file is relocated to `docs/port/` in
> slice S7 (D-006).

Each row maps an observed source behavior (from the TS_PORT_INDEX.md
`cli` feature fragment and `py_launch_blueprint/projects.py` at the
pinned SHA) to the port's behavior. "Same" means observably identical
(byte-identical where output shapes are concerned); deviations carry
the decision reference that ordered them.

| #   | Source behavior (py-projects)                                                                                                                                                                          | Port behavior (ts-projects)                                                                                                                                                           | Verdict                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| 1   | Bare `py-projects` runs the single command (projects.py:317)                                                                                                                                           | `projects` is the default Commander subcommand; bare `ts-projects` runs it (also callable as `ts-projects projects`)                                                                  | Same                                                                                                                 |
| 2   | Success (incl. benign empties) → exit 0 (projects.py:364-366, 383-385)                                                                                                                                 | Same paths → exit 0                                                                                                                                                                   | Same                                                                                                                 |
| 3   | Config error / missing token → exit 1 (projects.py:270, 351-353)                                                                                                                                       | Config error → 1; missing/invalid token → **4** (auth)                                                                                                                                | Deviation — D-016(2) (cli-standards R6.1 normative; mapping table in EXAMPLECLI.md)                                  |
| 4   | API/network failure (`PyError`) → exit 3 (projects.py:401-403)                                                                                                                                         | `ApiError` → **1** (general error)                                                                                                                                                    | Deviation — D-016(2) (API/network → 1)                                                                               |
| 5   | Workspace not found → `PyError` → exit 3 (projects.py:242)                                                                                                                                             | `NotFoundError` → exit 3                                                                                                                                                              | Same (code unchanged, now via the not-found class)                                                                   |
| 6   | Unexpected exception → exit 4, traceback only under `--verbose` (projects.py:404-408)                                                                                                                  | Unexpected → **1**; stack trace on stderr only at `-v`+/`--debug`                                                                                                                     | Deviation — D-016(2) (4 reserved for auth); traceback gating preserved (D-018(3))                                    |
| 7   | Usage errors → exit 2 (click default: bad flag value, nonexistent `--config`)                                                                                                                          | Exit 2 via Commander `exitOverride` (Commander's default 1 deliberately overridden)                                                                                                   | Same — D-016(2)                                                                                                      |
| 8   | No SIGINT/SIGTERM handling (doc-only "5 = user interrupt", never emitted)                                                                                                                              | SIGINT → 130, SIGTERM → 143 (verified e2e mid-fetch)                                                                                                                                  | Deviation (improvement) — D-016(2)                                                                                   |
| 9   | `--limit` default 200 (projects.py:322)                                                                                                                                                                | Default 200, pinned in tests; config-file `limit` may override the default, flag always wins                                                                                          | Same (config-file tier is additive, D-017(1))                                                                        |
| 10  | `--format` choice text\|json\|csv, default text, parser-level rejection (projects.py:323-328)                                                                                                          | Identical choices/default via Commander `Option.choices`; invalid value → exit 2                                                                                                      | Same                                                                                                                 |
| 11  | JSON shape `{"projects": [...]}`, 2-space indent (projects.py:284-285)                                                                                                                                 | `JSON.stringify({ projects }, null, 2)` — byte-identical                                                                                                                              | Same                                                                                                                 |
| 12  | CSV shape: literal `id,name` header, unquoted naive rows (projects.py:286-289)                                                                                                                         | Same header/rows; minimal RFC 4180 quoting applied ONLY when a value contains `,` `"` or newline                                                                                      | Same for clean values; deviation (improvement) for hostile values — fixes the source's naive-by-design escaping wart |
| 13  | Text shape: newline-joined project IDs (projects.py:290-291)                                                                                                                                           | Byte-identical                                                                                                                                                                        | Same                                                                                                                 |
| 14  | Token precedence `--token` > `PY_TOKEN` > `.env` file; QUIRK: missing env/file token errored before the `--token` flag was applied, so `--token` alone failed (projects.py:251-270 ran before 345-349) | `--token` > `TS_PROJECTS_TOKEN` > TOML config; validation runs AFTER all three layers merge, so `--token` alone works (the documented precedence, made true)                          | Deviation (quirk fixed) — D-017/S3a; the INDEX-documented intent is the precedence, not the bug                      |
| 15  | `.env` dotenv config file at `~/.config/py-cli/.env` (docs said `py-launch-blueprint`, binary said `py-cli` — drift)                                                                                   | TOML `~/.config/ts-projects/ts-projects_config.toml`, ONE name everywhere                                                                                                             | Deviation — D-017(1,5), D-029 (naming drift resolved)                                                                |
| 16  | Empty selection → yellow "No projects selected" notice, exit 0 (projects.py:383-385)                                                                                                                   | Same message, exit 0 — notice on **stderr** (source printed it to stdout)                                                                                                             | Same behavior; stream deviation — D-018(4)                                                                           |
| 17  | Empty fetch → yellow "No projects found." notice, exit 0 (projects.py:364-366)                                                                                                                         | Same message, exit 0, on stderr                                                                                                                                                       | Same behavior; stream deviation — D-018(4)                                                                           |
| 18  | Interactive checkbox multi-select, labels `"Name (Workspace)"`, values = full project objects (projects.py:372-381); `.ask()` on non-TTY undefined-ish, no bypass flag                                 | @inquirer/prompts `checkbox` with identical label/value shape, rendered on stderr; documented non-interactive path: `--no-input` or non-TTY stdin/stderr selects ALL fetched projects | Same intent; documented improvement — D-016(6)                                                                       |
| 19  | Pre-selection preview table (Project Name/Workspace, +ID under `--verbose`) in text mode, on stdout (projects.py:368-370, 294-314)                                                                     | cli-table3 with identical columns/verbose rule, on **stderr**, and only when interactive                                                                                              | Same columns; stream/gating deviation — D-016(4), D-018(4)                                                           |
| 20  | Progress spinner during fetch printed to **stdout**, contaminating piped JSON (tests regex-strip it; projects.py:358-362)                                                                              | yocto-spinner on **stderr**, only when stderr is a TTY and `CI` is unset; e2e asserts piped stdout parses with NO stripping                                                           | Deviation (mandated improvement) — D-018(5), D-016(5)                                                                |
| 21  | `--output <file>` writes the exact result string; green confirmation replaces data on stdout (projects.py:390-393)                                                                                     | Same file content (byte-identical, no added newline); confirmation on stderr                                                                                                          | Same behavior; stream deviation — D-018(4)                                                                           |
| 22  | `--copy` copies the exact formatted string via pyperclip; crashes on headless systems (projects.py:397-399)                                                                                            | clipboardy (dynamic import) receives the identical string; headless failure degrades to a clear stderr error + exit 1 AFTER the result reached its sink                               | Same payload contract; degradation deviation (improvement) — D-016(7)                                                |
| 23  | Workspace filter: case-insensitive name → `gid` via GET /workspaces, passed as `workspace` param (projects.py:234-244)                                                                                 | Identical resolution and request param                                                                                                                                                | Same                                                                                                                 |
| 24  | Request params `limit` + `opt_fields=name,workspace.name`; Bearer + Accept headers; BASE_URL `https://app.py.com/api/1.0`; no client-side pagination (single request)                                  | Identical params/headers/semantics; BASE_URL `https://app.ts.com/api/1.0` (D-029 naming); `TS_PROJECTS_API_URL` env override added for testability                                    | Same (renamed root; override documented in EXAMPLECLI.md)                                                            |
| 25  | API error message surfaces `errors[0].message` with `str(e)` fallback (projects.py:191-205)                                                                                                            | Same surfacing: `API request failed: <errors[0].message>`, HTTP-status fallback                                                                                                       | Same                                                                                                                 |
| 26  | `--no-color` declared but never read — dead flag (projects.py:331, 341)                                                                                                                                | Implemented for real: `--no-color` > `NO_COLOR` > `FORCE_COLOR` > TTY                                                                                                                 | Deviation (mandated fix) — D-016(3)/D-026, INDEX:1415-1423                                                           |
| 27  | Fuzzy search advertised (docstring, EXAMPLECLI features, thefuzz deps) but never implemented                                                                                                           | Claim removed from EXAMPLECLI/README/module docs; no fuzzy dependency shipped                                                                                                         | Deviation (claim dropped to match actual behavior) — D-031                                                           |
| 28  | `--version` prints package version, exit 0 (projects.py:318)                                                                                                                                           | Same (`ts-projects <version>` == package.json, pinned by test)                                                                                                                        | Same                                                                                                                 |
| 29  | Mis-mocked prompt tests silently pinned the empty-selection path (test_cli.py:82,100,155)                                                                                                              | DI prompter seam; BOTH paths pinned explicitly (populated → format/sinks, empty → exit 0)                                                                                             | Deviation in tests only (intent ported with correct seams) — D-019(5)                                                |
| 30  | Exits 3 and 4 defined but untested (INDEX known gap)                                                                                                                                                   | API-error, not-found, and auth exit paths all pinned in unit + e2e tiers                                                                                                              | Deviation (gap closed) — D-019(5)                                                                                    |
| 31  | Interrupt (^C) or stdin-close during the questionary checkbox: `.ask()` swallows KeyboardInterrupt and returns None → "No projects selected" → exit 0 (projects.py:378-385)                            | @inquirer's ExitPromptError caught centrally: clean `Cancelled.` on stderr (no library internals), exit **130** for both ^C and stdin-close mid-prompt                                | Deviation — D-033 (R6.1 interrupt contract; swallowing ^C into "success" hides cancellation from scripts)            |
| 32  | No HTTP timeout: `requests` waits indefinitely on a hung server (projects.py:186-205 passes no timeout)                                                                                                | 30s `AbortSignal.timeout` per request (`DEFAULT_TIMEOUT_MS`), surfaced as ApiError → exit 1                                                                                           | Deviation — D-033 (a hung placeholder API fails loudly instead of hanging the CLI)                                   |
| 33  | `--limit` accepts any integer — click coerces `0`/negatives and passes them straight to the API param (projects.py:322)                                                                                | `--limit` requires a positive integer; `0`/negative/non-numeric → usage error, exit 2                                                                                                 | Deviation — D-033 (parser-level validation instead of forwarding nonsense to the API)                                |
| 34  | Machine formats carry no error contract: failures print the same human text regardless of `--format` (projects.py:401-408); no `--json` alias                                                          | With json requested (`--format json` or the `--json` alias), errors after arg parsing emit a single `{"error":{"code","message"}}` object on stderr; text/csv keep human text         | Deviation — D-018(4)/D-033 (cli-standards R7.8 envelope; `--json` ≡ `--format json` per R4.2, `.conflicts` guarded)  |

**Summary**: 12 rows fully "Same"; 5 rows same behavior with the
notice/preview stream moved to stderr (single cause: D-018(4) stdout
purity); 17 rows deviate deliberately, every one ordered by a recorded
decision (exit-code remap D-016(2); TOML config D-017; stdout purity +
machine envelope D-018(4,5); dead-flag/fuzzy fixes D-026/D-031;
test-seam corrections D-019(5); prompt-interrupt 130, request timeout,
--limit validation, and the --json alias per the S3b gate adjudication
D-033).
